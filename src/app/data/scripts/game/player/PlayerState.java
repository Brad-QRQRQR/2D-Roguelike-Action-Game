package app.data.scripts.game.player;

public enum PlayerState {
    idle,
    run,
    jump,
    runJump,
    hit,
    criticalHit,
    block,
    blockHeal,
    parry,
    heldBlock,
    parryBurst,
    skillEntry,
    parryWithoutHit,
    invulnerability,
}
